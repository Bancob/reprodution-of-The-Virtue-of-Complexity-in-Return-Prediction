# requirements.txt for Project 1 (DSA5205)
# Python 3.10+

# Stdlib modules (no need to install): os, dataclasses, typing, concurrent.futures, multiprocessing

numpy~=1.26.4
pandas~=2.2.2
matplotlib~=3.8.4
scipy~=1.11.4
scikit-learn~=1.4.2

# Transitive deps of scikit-learn (listed explicitly to avoid resolver quirks)
joblib~=1.3.2
threadpoolctl~=3.3.0

# Optional (for interactive notebooks; not strictly required to run code)
# jupyterlab~=4.2.0
# ipykernel~=6.29.0